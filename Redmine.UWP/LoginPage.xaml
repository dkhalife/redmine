<Page
    x:Class="Redmine.UWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Redmine.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image Source="Assets/RedmineLogo.png" VerticalAlignment="Top" Margin="0,80,0,0" />

        <StackPanel Orientation="Vertical">
            <TextBlock>Server</TextBlock>
            <ListBox>
                <ListBoxItem>Add a New Server:</ListBoxItem>
            </ListBox>

            <TextBlock>Login Using:</TextBlock>
            <ListBox>
                <ListBoxItem>Account</ListBoxItem>
                <ListBoxItem>API Key</ListBoxItem>
            </ListBox>

            <TextBlock>Username:</TextBlock>
            <TextBox Name="Username"></TextBox>

            <TextBlock>Password:</TextBlock>
            <TextBox Name="Password"></TextBox>

            <TextBlock>API Key:</TextBlock>
            <TextBox Name="ApiKey"></TextBox>

            <StackPanel Orientation="Horizontal">
                <Button>Login</Button>
                <ProgressRing IsActive="True" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
